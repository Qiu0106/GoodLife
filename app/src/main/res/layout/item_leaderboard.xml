<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="@dimen/elderly_padding_card"
    android:gravity="center_vertical"
    android:layout_marginBottom="@dimen/elderly_spacing_xs"
    android:background="@color/elderly_surface"
    android:minHeight="@dimen/elderly_touch_min">

    <TextView
        android:id="@+id/tv_rank"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:text="1"
        android:textSize="@dimen/elderly_text_body_large"
        android:textStyle="bold"
        android:textColor="@color/elderly_text_primary"
        android:gravity="center" />

    <ImageView
        android:id="@+id/iv_avatar"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="@dimen/elderly_spacing_sm"
        android:src="@mipmap/ic_launcher_round"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="@dimen/elderly_spacing_md"
        android:text="用户"
        android:textSize="@dimen/elderly_text_body_large"
        android:textColor="@color/elderly_text_primary"
        android:ellipsize="end"
        android:maxLines="1" />

    <TextView
        android:id="@+id/tv_points"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="500分"
        android:textSize="@dimen/elderly_text_body"
        android:textColor="@color/elderly_brand"
        android:textStyle="bold" />
</LinearLayout>
