# 3.2.3 关键数据结构

## 1. 概述

本系统采用面向对象的程序设计方法，使用 Kotlin 数据类（data class）和普通类定义内存中的数据存储结构。数据模型分为两类：
- **前端数据模型**：用于 UI 展示和本地业务逻辑处理
- **Bmob 数据模型**：用于与 Bmob 后端云进行数据交互

## 2. 核心数据类图

### 2.1 用户模块数据类

```
User (data class)
├── id: String?              // 用户唯一标识
├── objectId: String?        // Bmob 对象ID
├── phone: String            // 手机号（主键）
├── password: String?        // 密码（仅传输，不存储）
├── nickname: String?        // 昵称
├── realName: String?        // 真实姓名
├── avatarUrl: String?       // 头像URL
├── gender: String?          // 性别（"男"/"女"/"保密"）
├── birthDate: String?       // 出生日期（格式：yyyy-MM-dd）
├── address: String?         // 居住地址
├── emergencyContact: String?    // 紧急联系人姓名
├── emergencyPhone: String?      // 紧急联系人电话
├── bloodType: String?       // 血型（A型/B型/AB型/O型/未知）
├── medicalHistory: String?  // 过敏史/慢性病
├── signature: String?       // 个性签名/寄语
├── token: String?           // Session Token
├── createdAt: String?       // 创建时间
├── updatedAt: String?       // 更新时间
└── syncStatus: Int          // 同步状态（0未同步/1已同步/2失败）

LoginRequest (data class)
├── phone: String
└── password: String

RegisterRequest (data class)
├── phone: String
├── password: String
└── nickname: String?

ApiResponse<T> (data class)
├── code: Int
├── message: String
└── data: T?
```

### 2.2 身后事计划模块数据类

```
AfterlifePlan (data class)
├── id: Long?                // 本地ID
├── userId: String           // 关联用户ID
├── name: String             // 逝者姓名
├── age: String              // 逝者年龄
├── contact: String          // 联系人电话
├── biography: String?       // 生平简介
├── funeralStyle: String     // 葬礼风格
├── funeralLocation: String? // 葬礼地点
├── funeralDate: String?     // 葬礼日期
├── funeralTime: String?     // 葬礼时间
├── relicsHandling: String   // 遗物处理方式
├── burialMethod: String     // 安葬方式
├── burialLocation: String?  // 安葬地点
├── coffin: Boolean          // 是否需要棺材
├── urn: Boolean             // 是否需要骨灰盒
├── flowers: Boolean         // 是否需要鲜花
├── candles: Boolean         // 是否需要蜡烛
├── photos: Boolean          // 是否需要遗像
├── otherSupplies: String?   // 其他用品
├── bgm: String              // 背景音乐
├── bgmNotes: String?        // 音乐备注
├── visitDate: String?       // 祭拜日期
├── visitTime: String?       // 祭拜时间
├── visitNotes: String?      // 祭拜备注
├── createdAt: String?
├── updatedAt: String?
└── syncStatus: Int

AfterlifePlanVisitInfo (data class)
├── id: Long?
├── planId: String           // 关联计划ID
├── userId: String           // 用户ID
├── visitDate: String?       // 祭拜日期
├── visitTime: String?       // 祭拜时间
├── visitNotes: String?      // 祭拜备注
├── createdAt: String?
└── updatedAt: String?
```

### 2.3 社区帖子模块数据类

```
Post (data class)
├── id: Long?
├── objectId: String?        // Bmob对象ID
├── userId: String           // 发布者ID
├── title: String            // 帖子标题
├── content: String          // 帖子内容
├── category: String         // 分类
├── coverImageUrl: String?   // 封面图片
├── imageUrls: String?       // 图片列表（JSON格式）
├── userName: String         // 发布者昵称
├── userAvatarUrl: String?   // 发布者头像
├── likeCount: Int           // 点赞数
├── favoriteCount: Int       // 收藏数
├── commentCount: Int        // 评论数
├── createdAt: String?
├── updatedAt: String?
└── syncStatus: Int

Comment (data class)
├── id: Long?
├── objectId: String?
├── postId: String           // 关联帖子ID
├── authorName: String       // 评论者昵称
├── authorAvatar: String?    // 评论者头像
├── content: String          // 评论内容
├── createdAt: String?
└── updatedAt: String?

PostInteraction (data class)
├── id: Long?
├── objectId: String?
├── userId: String           // 用户ID
├── postId: String           // 帖子ID
├── type: String             // 交互类型（like/favorite）
└── createdAt: String?
```

### 2.4 打卡签到模块数据类

```
CheckinRecord (data class)
├── id: Long?
├── objectId: String?
├── userId: String           // 用户ID
├── nickname: String         // 用户昵称
├── avatarUrl: String?       // 用户头像
├── totalPoints: Int         // 总积分
├── consecutiveDays: Int     // 连续签到天数
├── lastCheckinDate: String  // 最后签到日期
├── createdAt: String?
└── updatedAt: String?
```

### 2.5 纪念星模块数据类

```
MemorialData (data class)
├── id: String               // 纪念星ID
├── name: String             // 逝者姓名
├── lifeYears: String        // 生卒年（如"1945年 - 2023年"）
├── message: String          // 寄语
├── story: String            // 生平事迹
├── imageUrl: String?        // 头像URL
└── flowerCount: Int         // 献花次数
```

---

# 3.2.1 数据存储

## 1. 存储方式概述

本系统采用 **Bmob 后端云** 作为主要数据存储方案，所有业务数据均存储在 Bmob 云端数据库中。本地不存储敏感数据，仅使用内存缓存提升用户体验。

## 2. Bmob 数据库表结构

### 2.1 _User 表（用户表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| objectId | String | 系统自动生成的唯一标识 |
| username | String | 用户名（使用手机号） |
| mobilePhoneNumber | String | 手机号码 |
| mobilePhoneVerified | Boolean | 手机号是否验证 |
| email | String | 邮箱地址 |
| emailVerified | Boolean | 邮箱是否验证 |
| nickname | String | 用户昵称 |
| realName | String | 真实姓名 |
| avatarUrl | String | 头像URL |
| gender | String | 性别 |
| birthDate | String | 出生日期 |
| address | String | 居住地址 |
| emergencyContact | String | 紧急联系人姓名 |
| emergencyPhone | String | 紧急联系人电话 |
| bloodType | String | 血型 |
| medicalHistory | String | 过敏史/慢性病 |
| signature | String | 个性签名 |
| sessionToken | String | 登录会话令牌 |
| createdAt | Date | 创建时间 |
| updatedAt | Date | 更新时间 |

### 2.2 AfterlifePlan 表（身后事计划表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| objectId | String | 唯一标识 |
| userId | String | 关联用户ID |
| name | String | 逝者姓名 |
| age | String | 逝者年龄 |
| contact | String | 联系人电话 |
| biography | String | 生平简介 |
| funeralInfo | String | 葬礼信息（JSON格式） |
| relicsInfo | String | 遗物处理信息（JSON格式） |
| supplies | String | 殡葬用品（JSON格式） |
| bgmInfo | String | 背景音乐信息（JSON格式） |
| createdAt | Date | 创建时间 |
| updatedAt | Date | 更新时间 |

**JSON 字段说明：**
- **funeralInfo**: `{"style": "风格", "location": "地点", "date": "日期", "time": "时间"}`
- **relicsInfo**: `{"handling": "处理方式", "method": "安葬方式", "location": "安葬地点"}`
- **supplies**: `{"coffin": true, "urn": true, "flowers": true, "candles": true, "photos": true, "other": "其他"}`
- **bgmInfo**: `{"name": "音乐名称", "notes": "备注"}`

### 2.3 AfterlifePlan_V 表（祭拜信息表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| objectId | String | 唯一标识 |
| planId | String | 关联身后事计划ID |
| userId | String | 用户ID |
| visitDate | String | 祭拜日期 |
| visitTime | String | 祭拜时间 |
| visitNotes | String | 祭拜备注 |
| createdAt | Date | 创建时间 |
| updatedAt | Date | 更新时间 |

### 2.4 Post 表（社区帖子表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| objectId | String | 唯一标识 |
| userId | String | 发布者ID |
| title | String | 帖子标题 |
| content | String | 帖子内容 |
| category | String | 分类 |
| coverImageUrl | String | 封面图片URL |
| imageUrls | String | 图片列表（逗号分隔） |
| userName | String | 发布者昵称 |
| userAvatarUrl | String | 发布者头像URL |
| likeCount | Number | 点赞数 |
| favoriteCount | Number | 收藏数 |
| commentCount | Number | 评论数 |
| createdAt | Date | 创建时间 |
| updatedAt | Date | 更新时间 |

### 2.5 Comment 表（评论表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| objectId | String | 唯一标识 |
| postId | String | 关联帖子ID |
| authorName | String | 评论者昵称 |
| authorAvatar | String | 评论者头像 |
| content | String | 评论内容 |
| createdAt | Date | 创建时间 |
| updatedAt | Date | 更新时间 |

### 2.6 CheckinRecord 表（打卡记录表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| objectId | String | 唯一标识 |
| userId | String | 用户ID |
| nickname | String | 用户昵称 |
| avatarUrl | String | 用户头像 |
| totalPoints | Number | 总积分 |
| consecutiveDays | Number | 连续签到天数 |
| lastCheckinDate | String | 最后签到日期 |
| createdAt | Date | 创建时间 |
| updatedAt | Date | 更新时间 |

### 2.7 MemorialStar 表（纪念星表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| objectId | String | 唯一标识 |
| name | String | 逝者姓名 |
| lifeYears | String | 生卒年 |
| message | String | 寄语 |
| story | String | 生平事迹 |
| avatarUrl | String | 头像URL |
| flowerCount | Number | 献花次数 |
| createdBy | String | 创建者用户ID |
| status | String | 审核状态（pending/approved/rejected） |
| createdAt | Date | 创建时间 |
| updatedAt | Date | 更新时间 |

### 2.8 PostInteraction 表（帖子交互表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| objectId | String | 唯一标识 |
| userId | String | 用户ID |
| postId | String | 帖子ID |
| type | String | 交互类型（like/favorite） |
| createdAt | Date | 创建时间 |
| updatedAt | Date | 更新时间 |

## 3. 数据同步机制

- **syncStatus 字段**：用于标记数据同步状态
  - 0：未同步（本地新增/修改，待上传）
  - 1：已同步（与云端一致）
  - 2：同步失败（需重试）

- **冲突处理**：以云端数据为准，本地修改覆盖云端

## 4. 数据安全

- 用户密码通过 Bmob 自动加密存储
- 敏感操作（修改个人信息）需验证 Session Token
- 使用 HTTPS 传输数据
